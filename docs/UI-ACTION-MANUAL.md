# UI ACTION MANUAL (M-F foundation)

Auto-generated by the mini box overnight (job-20260908n, model qwen38-27b-iq3s), graded 2026-09-09.
Sources verified: API endpoints cited match the real backend (spot-checked).

﻿# User-Action Inventory â€” Part 1 of 4
**Surfaces:** Landing / Project Setup Wizard

## 1. Landing Surface (`Landing.tsx`)

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| L-01 | **Prompt textarea** (input) | `Landing` â†’ Composer box | Type text; `Enter` + `Meta/Ctrl` | Updates local `prompt` state. `Enter`+`Meta/Ctrl` invokes `handleFormat()`. | None. |
| L-02 | **"Write Script" button** | `Landing` â†’ Composer box footer | Click | Calls `handleFormat()`. <br>**Branch A (Local Parse):** If text looks like a Fountain script (starts with `INT.`/`EXT.` or title line + early heading), `POST /api/writing/scripts` with `{ raw_text }`. On success, calls `onScriptFormatted(...)` and navigates to doc view. <br>**Branch B (LLM Format):** `POST /api/writing/format` with `{ prompt }`. Listens for `format_progress` CustomEvent to update progress bar. On success, calls `onScriptFormatted(data)`. <br>**Error Handling:** 502/503/413 opens `LlmConnectModal`. Other errors set `formatError`. | `prompt` must be non-empty. `formatting` must be `false`. |
| L-03 | **"Model" button** | `Landing` â†’ Composer box footer | Click | Sets `showLlmConnect` to `true`. Clears `llmError`. Opens `LlmConnectModal`. | `formatting` must be `false` (button is disabled otherwise). |
| L-04 | **Format Progress Bar** (Passive UI) | `Landing` â†’ Below composer | N/A (Auto-updates) | Displays `formatProgress.message`, `current/total`, and percentage. Triggered by `format_progress` events during `handleFormat()`. | `formatting` is `true` AND `formatProgress` is not null. |
| L-05 | **Format Error Banner** (Passive UI) | `Landing` â†’ Below composer | N/A (Auto-displays) | Displays `formatError` message. | `formatError` is not null. |
| L-06 | **"Browse projects" button** | `Landing` â†’ Secondary actions | Click | Calls `onBrowseProjects()` callback (navigates to project list). | None. |
| L-07 | **"New empty project" button** | `Landing` â†’ Secondary actions | Click | Toggles `showNameInput` state. Reveals inline creation panel. | None. |
| L-08 | **Output Format Picker** (Control) | `Landing` â†’ Inline creation panel | Select preset/FPS/Dimensions | Updates `formatSelection` state. | `showNameInput` is `true`. |
| L-09 | **Project Name Input** | `Landing` â†’ Inline creation panel | Type text; `Enter` key | Updates `projectName` state. `Enter` key invokes `handleCreateEmpty()` if valid. | `showNameInput` is `true`. |
| L-10 | **"Create" button** | `Landing` â†’ Inline creation panel | Click | Calls `handleCreateEmpty()`. <br>1. `scriptsApi.create(projectName, '')` creates empty script. <br>2. `renderSettingsApi.set(created.id, formatSelection)` persists canvas settings. <br>3. Shows toast "Project created". <br>4. Calls `onProjectCreated(created)`. | `projectName` non-empty. `formatSelection.preset` selected. `creating` is `false`. |
| L-11 | **LlmConnectModal** (Modal) | `Landing` â†’ Overlay | Opened by L-02 (error) or L-03. | **Close:** `onClose` sets `showLlmConnect` false, clears error. <br>**Connect:** `onConnected` sets `showLlmConnect` false, clears error, and **automatically retries** `handleFormat()`. | `showLlmConnect` is `true`. |

---

## 2. Project Setup Wizard (`WorkflowWizard.tsx`)

### 2.1 Global Wizard Controls

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-01 | **Close Button (X)** | `WorkflowWizard` â†’ Top Right | Click | Calls `handleClose()`. <br>If `step > 0` and `step < 6`: Opens Close Confirmation Dialog. <br>Else: Calls `onClose()`. | None. |
| W-02 | **Step Indicator Dots** (Passive UI) | `WorkflowWizard` â†’ Header | N/A | Visual state: Green (completed/skipped), Blue (current), Gray (upcoming). | None. |
| W-03 | **"Back" button** | `WorkflowWizard` â†’ Bottom Nav | Click | Calls `handleBack()`. Decrements `step` by 1. | `step > 0`. |
| W-04 | **"Next" / "Continue" button** | `WorkflowWizard` â†’ Bottom Nav | Click | Calls `handleNext()`. Increments `step` by 1. | `canAdvance()` returns `true` (see logic below). |
| W-05 | **"Skip Music" button** | `WorkflowWizard` â†’ Bottom Nav (Step 5 only) | Click | Calls `handleSkipStep()`. Adds current step to `stepSkipped` set. Increments `step`. | `step === 5`. |
| W-06 | **Close Confirmation Dialog** (Modal) | `WorkflowWizard` â†’ Overlay | Opened by W-01. | **Cancel:** Closes dialog. <br>**Close:** Calls `onClose()`. | `showCloseConfirm` is `true`. |

**`canAdvance()` Logic:**
*   **Step 0:** `script !== null`
*   **Step 1:** `pipelineProgress.percent >= 100`
*   **Step 2:** `scenes.length > 0`
*   **Step 3:** `scenes.length > 0`
*   **Step 4:** `completedScenes === scenes.length` AND `scenes.length > 0`
*   **Step 5:** `true` (Optional)
*   **Step 6:** `false` (Final step)

### 2.2 Step 0: Script

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-10 | **Output Format Picker** | `WorkflowWizard` â†’ Step 0 (if no script) | Select preset | Updates `formatSelection` state. | `script` is `null`. |
| W-11 | **Title Input** | `WorkflowWizard` â†’ Step 0 (if no script) | Type text | Updates `newTitle` state. | `script` is `null`. |
| W-12 | **Script Content Textarea** | `WorkflowWizard` â†’ Step 0 (if no script) | Type text | Updates `newContent` state. | `script` is `null`. |
| W-13 | **"Create & Continue" button** | `WorkflowWizard` â†’ Step 0 (if no script) | Click | Calls `handleCreateScript()`. <br>1. `scriptsApi.create(newTitle, newContent)`. <br>2. `renderSettingsApi.set(created.id, formatSelection)`. <br>3. Calls `onScriptCreated(created)`. <br>4. Resets local form state. | `newTitle` and `newContent` non-empty. `formatSelection.preset` selected. `creating` is `false`. |

### 2.3 Step 1: Scenes (Pipeline)

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-20 | **Pipeline Progress UI** (Passive) | `WorkflowWizard` â†’ Step 1 | N/A | Displays `pipelineProgress.stage`, `message`, `percent`. Auto-updates via polling. | `step === 1`. |
| W-21 | **Auto-Start Pipeline** (System) | `WorkflowWizard` â†’ Step 1 | N/A (Auto) | If entering Step 1 with `script` present, `pipelineSubmitted` false, and `scenes.length === 0`: <br>1. Calls `onScriptSubmit(script)`. <br>2. Starts `setInterval` polling `scriptsApi.progress(script.id)` every 3s. <br>3. On 100%: Clears interval, calls `onScenesUpdated()`, auto-advances to Step 2. | `step === 1`, `script` exists, `!pipelineSubmitted`, `scenes.length === 0`. |
| W-22 | **"Skip â€” Create scenes manually" button** | `WorkflowWizard` â†’ Step 1 | Click | Calls `handleSkipScenes()`. <br>1. Clears polling interval. <br>2. Adds Step 1 to `stepSkipped`. <br>3. Sets `step` to 2. | `step === 1`. |

### 2.4 Step 2: Frames

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-30 | **Scene Frame Grid** (Passive/Display) | `WorkflowWizard` â†’ Step 2 | N/A | Displays scene cards. Shows image if `scene.initial_frame_url` exists, else placeholder. Shows count of assigned frames. | `step === 2`. |
| W-31 | **Drag/Drop Frame** (Implied) | `WorkflowWizard` â†’ Step 2 | Drag from Sidebar | *Note: Source code indicates interaction with "Frames tab in sidebar" but specific drop handler is not in this component. Likely handled by parent/Sidebar.* | `step === 2`. |

### 2.5 Step 3: Voice

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-40 | **Scene Audio List** (Passive/Display) | `WorkflowWizard` â†’ Step 3 | N/A | Displays scene cards with audio status ("Audio Ready" / "No Audio"). Shows count of scenes with audio. | `step === 3`. |

### 2.6 Step 4: Render

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-50 | **SlotsPanel** (Component) | `WorkflowWizard` â†’ Step 4 | N/A | Renders slot management UI. | `step === 4`. |
| W-51 | **"Submit All" button** | `WorkflowWizard` â†’ Step 4 | Click | Calls `handleRenderAll()`. <br>1. `comfyuiApi.submitAll(script.id)`. <br>2. Calls `onScenesUpdated()`. | `script` exists. `scenes.length > 0`. |
| W-52 | **Render Status Polling** (System) | `WorkflowWizard` â†’ Step 4 | N/A (Auto) | If `step === 4`: <br>1. `setInterval` polls `comfyuiApi.pollStatus(script.id)` every 5s. <br>2. Calls `onScenesUpdated()`. <br>3. Stops polling when all scenes are `complete` or `error`. | `step === 4`, `script` exists. |
| W-53 | **Scene Status Badges** (Passive) | `WorkflowWizard` â†’ Step 4 | N/A | Displays status per scene: `complete` (green), `rendering` (orange pulse), `error` (red), `queued` (yellow). | `step === 4`. |

### 2.7 Step 5: Music

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-60 | **MusicPanel** (Component) | `WorkflowWizard` â†’ Step 5 | Select Track | Updates `selectedMusicTrack` state. | `step === 5`. |

### 2.8 Step 6: Export

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| W-70 | **ExportPanel** (Component) | `WorkflowWizard` â†’ Step 6 | N/A | Renders export controls. Calls `onExportComplete` upon success. | `step === 6`, `script` exists. |


---

﻿# User-Action Inventory (Part 2 of 4)

## Surface: Script Library

| ID | Component | Control / Interaction | User Trigger | Effect (API / State) | Preconditions |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **SL-01** | `ScriptLibrary` | Tab: "Library" | Click | Sets `mode` to `'library'`. Displays search bar, pipeline progress (if active), and script list. | None |
| **SL-02** | `ScriptLibrary` | Tab: "Create" | Click | Sets `mode` to `'create'`. Displays title input, script content textarea, and create button. | None |
| **SL-03** | `ScriptLibrary` | Search Input | Type | Filters `scripts` array client-side based on title, content, or tags matching the query. | `mode` is `'library'` |
| **SL-04** | `ScriptLibrary` | Script Card (Row) | Click | Calls `onSelect(script)`. Sets `activeScriptId` (handled by parent). | `mode` is `'library'` |
| **SL-05** | `ScriptLibrary` | Button: "Submit" | Click | Calls `onSubmitScript(script)`. Prevents propagation to card click. | `script.status` is `'ready'` or `'draft'` |
| **SL-06** | `ScriptLibrary` | Button: "Settings" | Click | Calls `onOpenSettings(script)`. Prevents propagation to card click. | `script` exists in list |
| **SL-07** | `ScriptLibrary` | Button: "x" (Delete) | Click | Calls `onDeleteScript(script)`. Prevents propagation to card click. | `script` exists in list |
| **SL-08** | `ScriptLibrary` | Input: "Title" | Type | Updates local `title` state. | `mode` is `'create'` |
| **SL-09** | `ScriptLibrary` | Textarea: "Script Content" | Type | Updates local `content` state. | `mode` is `'create'` |
| **SL-10** | `ScriptLibrary` | Button: "Create Project" | Click | 1. Validates `title` and `content` are non-empty.<br>2. Calls `scriptsApi.create(title, content)`. <br>3. On success: clears inputs, sets `mode` to `'library'`, calls `onCreated()`. <br>4. On error: displays error message. | `mode` is `'create'`<br>`title` and `content` are non-empty<br>`creating` is `false` |

## Surface: Script Document

| ID | Component | Control / Interaction | User Trigger | Effect (API / State) | Preconditions |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **SD-01** | `ScriptDoc` | Button: "Back" | Click | Calls `onBack()`. Navigates away from document view. | None |
| **SD-02** | `ScriptDoc` | Input: Title | Type | Updates local `title` state. Sets `dirty` to `true`. | None |
| **SD-03** | `ScriptDoc` | Button: "Cast" | Click | 1. Toggles `showCast` state.<br>2. If `voices` array is empty, fetches `/api/audio/voices` and populates `voices`. | None |
| **SD-04** | `ScriptDoc` | Button: "Save" | Click | 1. Regenerates `raw_text` from `lines`.<br>2. If `scriptId` exists: `PUT /api/writing/scripts/{scriptId}`.<br>3. If `scriptId` is null: `POST /api/writing/scripts`.<br>4. Updates `scriptId`, `version`, `lines`, `characters` from response.<br>5. Sets `dirty` to `false`. Calls `onSaved()`. | `dirty` is `true`<br>`saving` is `false` |
| **SD-05** | `ScriptDoc` | Line Text (Display) | Click | Sets `editingSeq` to the line's `seq`. Switches view to textarea for that line. | None |
| **SD-06** | `ScriptDoc` | Line Textarea (Edit) | Type | Updates `lines` array for the specific `seq`. Sets `dirty` to `true`. | `editingSeq` matches current line `seq` |
| **SD-07** | `ScriptDoc` | Line Textarea (Edit) | Key: `Enter` (no Shift) | Sets `editingSeq` to `null`. Exits edit mode. | `editingSeq` matches current line `seq` |
| **SD-08** | `ScriptDoc` | Line Textarea (Edit) | Key: `Escape` | Sets `editingSeq` to `null`. Exits edit mode. | `editingSeq` matches current line `seq` |
| **SD-09** | `ScriptDoc` | Line Textarea (Edit) | Blur | Sets `editingSeq` to `null`. Exits edit mode. | `editingSeq` matches current line `seq` |
| **SD-10** | `ScriptDoc` | Select: Line Type (Hover) | Change | Calls `flipType(seq, newType)`. <br>â€¢ If `dialogue`: Finds preceding character cue. If none, assigns `NARRATOR` and inserts cue if missing.<br>â€¢ If `direction`/`heading`: Clears `character_id`.<br>Sets `dirty` to `true`. | Line is hovered |
| **SD-11** | `ScriptDoc` | Button: "x" (Delete Line) | Click | Calls `deleteLine(seq)`. Removes line from `lines` array. Sets `dirty` to `true`. | Line is hovered |

## Surface: Project Settings

| ID | Component | Control / Interaction | User Trigger | Effect (API / State) | Preconditions |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **PS-01** | `ProjectSettingsModal` | Button: "x" (Close) | Click | Calls `onClose()`. Dismisses modal. | None |
| **PS-02** | `ProjectSettingsModal` | Preset Selection (via `OutputFormatPicker`) | Click/Select | 1. Checks grading verdict.<br>2. If `exceeds`: Shows `ChangeWarningDialog` (Exceeds variant).<br>3. If scenes affected: Shows `ChangeWarningDialog` (Change variant).<br>4. Else: Calls `apply(preset)`. | Modal is open |
| **PS-03** | `ProjectSettingsModal` | `apply` (Internal) | Programmatic | 1. If `custom`: Validates width/height/fps.<br>2. Calls `renderSettingsApi.set(scriptId, body)`. <br>3. Updates `current`, `selected`. Shows "Saved" flash. Calls `onSaved()`. | Preset selected |
| **PS-04** | `ProjectSettingsModal` | FPS Selection (via `OutputFormatPicker`) | Click/Select | 1. If custom FPS > `fpsCap`: Shows `window.confirm` warning.<br>2. If confirmed: Calls `renderSettingsApi.set(scriptId, { preset, fps })`. <br>3. Updates `current`. Shows "Saved" flash. Calls `onSaved()`. | Modal is open |
| **PS-05** | `ChangeWarningDialog` | Button: "Cancel" | Click | 1. Sets `pendingConfirm` to `null`. <br>2. Resets `selected` to `current.preset`. | Warning dialog is visible |
| **PS-06** | `ChangeWarningDialog` | Button: "Use anyway" / "Change format" | Click | Calls `apply(pending.preset)`. Proceeds with saving the new format despite warnings. | Warning dialog is visible |


---

﻿# User-Action Inventory â€” Part 3 of 4

## Surface: Timeline

| # | Component | Control / Interaction | User Trigger | Effect (API / State) | Preconditions |
|---|-----------|-----------------------|--------------|----------------------|-------------|
| T1 | `Timeline` | **Settings** button (header) | Click | Calls `onOpenProjectSettings()` â†’ opens project settings modal (resolution + FPS) | Always available |
| T2 | `Timeline` | **Export** button (header) | Click | Calls `onExport()` â†’ triggers project export pipeline | `scenes.length > 0` (button disabled when 0) |
| T3 | `Timeline` | **Submit All Ready** button (header) | Click | Calls `onSubmitAll()` â†’ submits all scenes in `ready`, `draft`, or `error` status for rendering | Always available (no-op if no eligible scenes) |
| T4 | `Timeline` | **Insert (+) button** â€” before first scene | Click | Calls `onInsert(null)` â†’ inserts a new scene at index 0 | `scenes.length > 0` (empty state shows no timeline) |
| T5 | `Timeline` | **Insert (+) button** â€” between scenes | Click | Calls `onInsert(scene.id)` â†’ inserts a new scene after the given scene | `scenes.length > 0` |
| T6 | `Timeline` | **Drag-to-scroll** (mouse drag on timeline background) | Mouse down + drag on scrollable area (excludes buttons, inputs, textareas, selects, links, videos, draggable elements) | Scrolls `scrollRef` horizontally; multiplier 1.8Ã—; cursor changes to `grabbing`; `user-select: none` | No card reorder drag in progress (`draggedSceneId` is null) |
| T7 | `Timeline` | **Scroll bar** (range input, 0â€“100) | Drag slider / arrow keys | Calls `scrollToPercent(p)` â†’ smooth-scrolls timeline to `p%` of max scroll width; updates `scrollProgress` state | `scenes.length > 0` |
| T8 | `Timeline` | **Scroll bar** percentage display | (passive) | Displays `Math.round(scrollProgress)%` | `scenes.length > 0` |
| T9 | `Timeline` | **Card reorder drag â€” start** | Drag a scene card (HTML5 `draggable`) | Sets `draggedSceneId`; card opacity â†’ 0.4, scale â†’ 0.95; starts auto-scroll interval (16 ms); adds document-level `dragover` listener for edge tracking | Card is rendered in timeline |
| T10 | `Timeline` | **Card reorder drag â€” hover over target card** | Drag over another scene card | Determines drop position by horizontal ratio: `< 0.3` â†’ `before`, `> 0.7` â†’ `after`, else `on`; sets `dropTargetId` and `dropPosition`; shows `GapIndicator` or swap ring on target | `draggedSceneId` is set and differs from hovered card; data type is NOT `application/x-frame` |
| T11 | `Timeline` | **Card reorder drag â€” drop (insert before/after)** | Release drag on target card (position â‰  `on`) | Builds new `sceneIds` array; calls `onReorder(newIds)`; clears drag state; stops auto-scroll | Drop is not a no-op (card not already at that position) |
| T12 | `Timeline` | **Card reorder drag â€” drop (swap)** | Release drag on target card (position = `on`) | Swaps dragged and target indices in `sceneIds`; calls `onReorder(newIds)`; clears drag state; stops auto-scroll | `draggedSceneId` â‰  `targetSceneId` |
| T13 | `Timeline` | **Card reorder drag â€” no-op drop** | Release drag where card would return to its own position | Clears drag state; stops auto-scroll; **no** `onReorder` call | Computed new order === current order |
| T14 | `Timeline` | **Card reorder drag â€” end (no drop)** | Release drag outside any valid target / press Esc | Restores card opacity to 1; clears `draggedSceneId`, `dropTargetId`, `dropPosition`; stops auto-scroll; removes document-level `dragover` listener | Drag was initiated |
| T15 | `Timeline` | **Auto-scroll during card drag** (passive) | Mouse within 100 px of left/right edge of timeline while dragging a card | `setInterval` (16 ms) scrolls container by up to 25 px/frame (speed scales with proximity to edge); syncs `scrollProgress` | `draggedSceneId` is set; mouse Y is within timeline vertical bounds |
| T16 | `Timeline` | **Gap indicator** (visual) | (passive â€” appears during drag) | Shows dashed-border placeholder at active boundary when `gapBoundary` is computed and not a no-op | `draggedSceneId` + `dropTargetId` + `dropPosition` (`before`/`after`) are set; not a no-op |
| T17 | `Timeline` | **Empty state** (passive) | (rendered when `scenes.length === 0`) | Displays "No scenes yet" message; no interactive controls | `scenes.length === 0` |

---

## Surface: Scene Card

| # | Component | Control / Interaction | User Trigger | Effect (API / State) | Preconditions |
|---|-----------|-----------------------|--------------|----------------------|-------------|
| S1 | `SceneCard` | **Render version selector** â€” version button (`v1`, `v2`, â€¦) | Click | Calls `onSwitchRender(scene.id, renderId)` â†’ sets that render as active | `scene.status === 'complete'` AND `scene.renders.length > 1` |
| S2 | `SceneCard` | **Render version selector** â€” delete button (`x`) | Click | Calls `onDeleteRender(scene.id, renderId)` â†’ removes that render version | `scene.status === 'complete'` AND `scene.renders.length > 1` |
| S3 | `SceneCard` | **Video player** â€” play/pause (click on video or overlay) | Click | Toggles `video.play()` / `video.pause()`; updates `videoPlaying` state; overlay icon swaps (play triangle â†” pause bars) | `scene.status === 'complete'` AND `videoUrl` is non-null |
| S4 | `SceneCard` | **Video player** â€” end of video (passive) | Video reaches end | `onEnded` fires â†’ `setVideoPlaying(false)` | Video was playing |
| S5 | `SceneCard` | **Initial frame image** â€” click to replace | Click on frame image | Opens hidden file input (`frameInputRef`) â†’ user selects image â†’ `onFrameUpload(scene.id, file)` | `scene.initial_frame_url` exists AND `scene.status !== 'complete'` (or no video URL) |
| S6 | `SceneCard` | **Initial frame image** â€” drag-and-drop frame from Frames tab | Drag `application/x-frame` data onto frame area | `onFrameAssign(frame, scene.id)` â†’ assigns dragged frame to this scene | `scene.initial_frame_url` exists; drag data type is `application/x-frame` |
| S7 | `SceneCard` | **Frame placeholder** â€” click to upload | Click on dashed placeholder | Opens hidden file input â†’ `onFrameUpload(scene.id, file)` | `scene.initial_frame_url` is null AND no video URL |
| S8 | `SceneCard` | **Frame placeholder** â€” drag-and-drop frame from Frames tab | Drag `application/x-frame` data onto placeholder | `onFrameAssign(frame, scene.id)` | `scene.initial_frame_url` is null; drag data type is `application/x-frame` |
| S9 | `SceneCard` | **Frame placeholder** â€” drag-over visual feedback (passive) | Drag enters placeholder area | `setDragOverFrame(true)` â†’ border turns blue, text changes to "Drop frame here" | Drag in progress |
| S10 | `SceneCard` | **Render progress bar** (passive) | (rendered during rendering) | Displays `scene.render_progress` %, elapsed time, estimated remaining; orange pulsing bar | `scene.status === 'rendering'` |
| S11 | `SceneCard` | **Cancel render** button (rendering state) | Click | Calls `onCancelRender(scene.id)` â†’ cancels active ComfyUI render job | `scene.status === 'rendering'` |
| S12 | `SceneCard` | **Queued status bar** (passive) | (rendered when queued) | Displays "Queued" + estimated remaining time; yellow bar at 3% | `scene.status === 'queued'` |
| S13 | `SceneCard` | **Cancel render** button (queued state) | Click | Calls `onCancelRender(scene.id)` â†’ removes scene from render queue | `scene.status === 'queued'` |
| S14 | `SceneCard` | **Error message** (passive) | (rendered on error) | Displays `scene.error_message` in red box | `scene.status === 'error'` AND `scene.error_message` is non-empty |
| S15 | `SceneCard` | **Prompt â€” Edit** button | Click | Sets `editingPrompt = true`; populates `promptDraft` with current `scene.prompt` | `editingPrompt` is false |
| S16 | `SceneCard` | **Prompt â€” textarea** (edit mode) | Type / edit text | Updates `promptDraft` state (local only, no API call until Save) | `editingPrompt` is true |
| S17 | `SceneCard` | **Prompt â€” Save** button | Click | Calls `onPromptChange(scene.id, promptDraft)` â†’ persists new prompt; sets `editingPrompt = false` | `editingPrompt` is true |
| S18 | `SceneCard` | **Prompt â€” Cancel** button | Click | Resets `promptDraft` to `scene.prompt`; sets `editingPrompt = false` | `editingPrompt` is true |
| S19 | `SceneCard` | **Prompt display** (passive) | (rendered when not editing) | Shows `scene.prompt` (clamped to 4 lines) or italic placeholder "No prompt yet â€” pipeline will generate one" | `editingPrompt` is false |
| S20 | `SceneCard` | **Dialogue preview** (passive) | (rendered when dialogue exists) | Displays `scene.dialogue` in italic, clamped to 2 lines | `scene.dialogue` is non-empty |
| S21 | `SceneCard` | **Audio â€” Generate** button | Click | Toggles `showVoiceSelect` â†’ reveals voice selection dropdown | Always available |
| S22 | `SceneCard` | **Audio â€” voice dropdown** (`<select>`) | Select a voice option | Sets `selectedVoice` state to chosen `voice_id` | `showVoiceSelect` is true; `voiceOptions` loaded (via `audioApi.listVoices()` on mount, cached) |
| S23 | `SceneCard` | **Audio â€” Go** button (voice selection) | Click | Calls `onGenerateAudio(scene.id, selectedVoice \|\| undefined)` â†’ triggers VoxCPM2 audio generation; hides voice selector | `showVoiceSelect` is true |


---

﻿# User-Action Inventory (Part 4a of 4)

## Surface: Export

| # | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|
| E1 | `ExportPanel` â€“ Music Track `<select>` | Change selection | Sets `selectedTrackId` state. If a track is selected, the Music Volume slider appears. | `musicTracks` loaded via `musicApi.list()` on mount. |
| E2 | `ExportPanel` â€“ Music Volume `<input type="range">` | Drag slider | Sets `musicVolume` state (0â€“1, step 0.05). Display updates to percentage. | A music track must be selected (`selectedTrackId` non-empty). |
| E3 | `ExportPanel` â€“ Include B-roll `<input type="checkbox">` | Toggle checkbox | Sets `includeBroll` state (boolean). | None. |
| E4 | `ExportPanel` â€“ Resolution `<select>` | Change selection | Sets `resolution` state (`720p`, `1080p`, or `original`). | None. |
| E5 | `ExportPanel` â€“ Output Name `<input type="text">` | Type text | Sets `outputName` state. | None. |
| E6 | `ExportPanel` â€“ "Assemble & Export" `<button>` | Click | 1. Sets `assembling=true`, clears `error`, initializes `progress`. 2. Calls `exportApi.assemble(scriptId, { music_track_id, music_volume, include_broll, output_name, resolution })`. 3. Starts 2s interval polling `exportApi.progress(scriptId)`. 4. On 100%: clears interval, calls `exportApi.listExports(scriptId)`, sets `completedExport`, calls `onExportComplete`. 5. On error: sets `error`, `assembling=false`. | `totalScenes > 0` and `assembling === false`. |
| E7 | `ExportPanel` â€“ Completed Export `<video>` | Play/Pause/Seek (native controls) | Plays the exported video from `completedExport.url`. | Export must have completed successfully. |
| E8 | `ExportPanel` â€“ "Download" `<a>` (completed export) | Click | Triggers browser download of `completedExport.url` with `completedExport.filename`. | Export must have completed successfully. |
| E9 | `ExportPanel` â€“ Previous Exports "Download" `<a>` | Click | Triggers browser download of `exp.url` with `exp.filename`. | `exports` list is non-empty. |
| E10 | `ExportPanel` â€“ Previous Exports "Delete" `<button>` | Click | 1. Calls `exportApi.deleteExport(scriptId, filename)`. 2. Calls `loadExports()` to refresh list. 3. On error: sets `error`. | `exports` list is non-empty. |

## Surface: Slots

| # | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|
| S1 | `SlotsPanel` â€“ Header `<button>` ("Edit"/"Close") | Click | Toggles `expanded` state. When expanded, loads project settings and renders slot controls. | `autoLoad` prop is `true` (default). |
| S2 | `SlotsPanel` â€“ Frame Size preset `<button>` (per preset key) | Click | 1. Calls `renderSettingsApi.set(scriptId, { preset })`. 2. If preset is `custom`, validates `customW`, `customH`, `customFps` are non-zero. 3. Sets `current` from response. 4. Shows "Saved" flash for 1.5s. 5. Calls `loadSlots()` to refresh template slots. 6. On error: sets `error`. | `scriptId` is provided. `savingPreset === false`. |
| S3 | `SlotsPanel` â€“ Custom Width `<input type="number">` | Type text | Sets `customW` state. | `current.preset === 'custom'`. |
| S4 | `SlotsPanel` â€“ Custom Height `<input type="number">` | Type text | Sets `customH` state. | `current.preset === 'custom'`. |
| S5 | `SlotsPanel` â€“ "Apply" `<button>` (custom size) | Click | Calls `applyPreset('custom')` with current `customW`, `customH`, `customFps`. Same effect as S2. | `current.preset === 'custom'`. `savingPreset === false`. |
| S6 | `SlotsPanel` â€“ "Custom size instead" `<button>` | Click | Calls `applyPreset('custom')`. Switches preset to custom, revealing custom W/H inputs. | `current.preset !== 'custom'`. `savingPreset === false`. |
| S7 | `SlotsPanel` â€“ Frame Rate `<button>` (per fps option) | Click | 1. Calls `renderSettingsApi.set(scriptId, { preset: current?.preset, fps })`. 2. Sets `current` from response. 3. Shows "Saved" flash for 1.5s. 4. On error: sets `error`. | `scriptId` is provided. `savingPreset === false`. |
| S8 | `SlotsPanel` â€“ Editable Slot Bool Toggle `<button>` | Click | 1. Calls `comfyuiApi.setSlot(slot.node_id, !currentValue)`. 2. Updates local `slots` state. 3. Shows "Saved" flash for 1.5s. 4. On error: sets `error`, calls `loadSlots()` to resync. | Slot is `editable === true`. `saving === null`. |
| S9 | `SlotsPanel` â€“ Editable Slot Number `<input type="number">` | Blur or press Enter | 1. Validates value is within `slot.min`/`slot.max` and changed. 2. Calls `comfyuiApi.setSlot(slot.node_id, newValue)`. 3. Updates local `slots` state. 4. Shows "Saved" flash for 1.5s. 5. On error: sets `error`, calls `loadSlots()` to resync. | Slot is `editable === true` and type is `int` or `float`. `saving === null`. |
| S10 | `SlotsPanel` â€“ "Reload from workflow" `<button>` | Click | Calls `loadSlots()` â†’ `comfyuiApi.slots()`. Refreshes `slots` state. | `expanded === true`. |

## Surface: Music

| # | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|
| M1 | `MusicPanel` â€“ Track Card `<div>` | Click | Calls `onSelectTrack(trackId)` or `onSelectTrack(null)` to toggle selection. Updates visual highlight. | Track exists in `tracks` list. |
| M2 | `MusicPanel` â€“ Play/Pause `<button>` | Click | 1. If playing: pauses `audioRef`, sets `playingId=null`. 2. If not playing: sets `audioRef.src = track.url`, plays audio, sets `playingId = track.track_id`. | Track exists in `tracks` list. |
| M3 | `MusicPanel` â€“ "Delete" `<button>` (per track) | Click | 1. Calls `musicApi.delete(trackId)`. 2. If deleted track was selected, calls `onSelectTrack(null)`. 3. Calls `loadTracks()` to refresh list. 4. On error: sets `error`. | Track exists in `tracks` list. |
| M4 | `MusicPanel` â€“ "Choose File" `<button>` | Click | Opens native file picker (`fileInputRef.current.click()`). | None. |
| M5 | `MusicPanel` â€“ "Upload" `<button>` | Click | 1. Validates file and title exist. 2. Calls `musicApi.upload(file, title, tags)`. 3. Clears title/tags/file input. 4. Calls `loadTracks()`. 5. On error: sets `error`. | `uploadTitle` is non-empty. `uploading === false`. File selected via M4. |
| M6 | `MusicPanel` â€“ "Generate Music" toggle `<button>` | Click | Toggles `showGenerate` state. Reveals/hides generation form. | None. |
| M7 | `MusicPanel` â€“ Generation Prompt `<input type="text">` | Type text | Sets `genPrompt` state. | `showGenerate === true` and a provider is configured. |
| M8 | `MusicPanel` â€“ Generation Duration `<input type="number">` | Type number | Sets `genDuration` state (5â€“300 sec). | `showGenerate === true` and a provider is configured. |
| M9 | `MusicPanel` â€“ "Generate" `<button>` | Click | 1. Validates prompt is non-empty. 2. Calls `musicApi.generate(prompt, duration)`. 3. Clears prompt. 4. Calls `loadTracks()`. 5. On error: sets `error`. | `genPrompt` is non-empty. `generating === false`. A music generation provider is configured. |
| M10 | `MusicPanel` â€“ "Provider Settings" toggle `<button>` | Click | Toggles `showProviders` state. Reveals/hides provider configuration form. | None. |
| M11 | `ProviderSettings` â€“ Active Provider `<select>` | Change selection | Sets `activeProvider` state. Loads endpoint/API key for the selected provider into local state. | `showProviders === true`. Provider config is loaded. |
| M12 | `ProviderSettings` â€“ Endpoint `<input type="text">` | Type text | Sets `endpoint` state. | `showProviders === true`. |
| M13 | `ProviderSettings` â€“ API Key `<input type="password">` | Type text | Sets `apiKey` state. | `showProviders === true`. |
| M14 | `ProviderSettings` â€“ "Save Provider Settings" `<button>` | Click | 1. Calls `musicApi.updateProviders(config)` with updated `active_provider`, `endpoint`, `api_key`. 2. Sets `providerConfig` from response. 3. On error: sets `error`. | `showProviders === true`. |


---

﻿# User-Action Inventory â€” Part 4b of 4

## Surface: Cast Panel

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| C-01 | **Auto-assign** button | `CastPanel` header | Click | `POST /api/writing/scripts/{scriptId}/versions/{version}/autocast` â†’ on success: toast "Auto-cast N character(s)", calls `onChanged()`. On failure: error toast. Button label toggles to "Castingâ€¦" while in-flight. | `autoCasting === false` (button disabled otherwise). Script and version must be valid. |
| C-02 | **Clear voice** button (per character) | `CastPanel` character card, top-right | Click | `PATCH /api/writing/scripts/{scriptId}/versions/{version}/characters/{characterId}` with body `{ voice_id: null }` â†’ calls `onChanged()`. On failure: error toast. | Character must have a `voice_id` assigned (button only rendered when `voice` is non-null). |
| C-03 | **Voice selector** `<select>` (per character) | `CastPanel` character card | Change selection | `PATCH /api/writing/scripts/{scriptId}/versions/{version}/characters/{characterId}` with body `{ voice_id: <selected> }` or `{ voice_id: null }` (when "â€” pick a voice â€”" chosen) â†’ calls `onChanged()`. On failure: error toast. | Voice catalog (`voices` prop) must be populated. |
| C-04 | **Preview** button (per character) | `CastPanel` character card, actions row | Click | `POST /api/writing/voice-preview` with body `{ text, voice_id, character_name }` â†’ on success: sets `previewUrl` and `previewChar`, renders `<audio>` player with `autoPlay`. On failure: error toast. Button label toggles to "Generatingâ€¦" while in-flight. | `previewing === null` (button disabled if any preview is in flight). Character must have a `voice_id` (otherwise preview will fail server-side). |
| C-05 | **Lock** checkbox (per character) | `CastPanel` character card, actions row | Toggle | `PATCH /api/writing/scripts/{scriptId}/versions/{version}/characters/{characterId}` with body `{ voice_locked: <bool> }` â†’ calls `onChanged()`. On failure: error toast. | None. |
| C-06 | **Audio preview player** | `CastPanel` character card (conditional) | Play / pause / seek (native `<audio controls>`) | Browser plays the generated preview URL (`audio/mpeg`). No API call. | `previewChar === c.id`, `previewUrl` is set, and `previewing === null` (preview generation complete). |
| C-07 | **Empty-state message** | `CastPanel` character list | *(passive â€” no trigger)* | Displays "No characters detected in this script." | `characters.length === 0`. |

---

## Surface: Frame Catalog

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| F-01 | **Upload** button | `FrameCatalog` header | Click | Opens hidden `<input type="file" accept="image/*">`. On file selection: calls `onUpload(file)` prop. Input value is reset after selection. | None. |
| F-02 | **Search** text input | `FrameCatalog` filter bar | Type / change value | Filters `frames` array client-side: matches query against `description`, `title`, and `tags` (case-insensitive). No API call. | None. |
| F-03 | **Scene selector** `<select>` | `FrameCatalog` filter bar | Change selection | Sets `selectedScene` state. No API call. Enables click-to-assign on frame cards. | `sceneIds` prop must be populated. |
| F-04 | **Drag frame to scene** (drag-and-drop) | `FrameCatalog` frame card | Drag card onto a scene card (drop target handled by parent) | `onDragStart` sets `dataTransfer` with `application/x-frame` = JSON-serialized frame object; `effectAllowed = 'copy'`. Drop is handled by the parent scene component (not in this file). | Frame must exist in `frames` array. |
| F-05 | **Click frame to assign** | `FrameCatalog` frame card | Click | Calls `onAssign(frame, selectedScene)` prop. No API call in this component. | `selectedScene` must be non-empty (a scene must be selected in F-03). |
| F-06 | **Empty-state message** | `FrameCatalog` grid | *(passive â€” no trigger)* | Displays "No frames available. Upload reference images to get started." | `filtered.length === 0`. |

---

## Surface: LLM Connect Modal

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| L-01 | **Modal open** (auto) | `LlmConnectModal` | *(triggered by parent on LLM failure)* | On mount: calls `llmApi.listProviders()` â†’ populates `providers` and `current`. Auto-selects first local provider and its first model if available. | Modal must be rendered by parent. |
| L-02 | **Close (Ã—)** button | `LlmConnectModal` header | Click | Calls `onClose()` prop. No API call. | None. |
| L-03 | **Backdrop click** | `LlmConnectModal` overlay | Click on backdrop (outside modal body) | Calls `onClose()` prop. No API call. | None. |
| L-04 | **Provider selection** button (per provider) | `LlmConnectModal` provider list | Click | Sets `selected` to the clicked provider; sets `selectedModel` to that provider's first model. No API call. | Provider list must be loaded (`loading === false`). |
| L-05 | **Model selection** button (per model) | `LlmConnectModal` model grid | Click | Sets `selectedModel` to the clicked model ID. No API call. | A provider must be selected (`selected !== null`). |
| L-06 | **Connect** button | `LlmConnectModal` actions row | Click | Calls `llmApi.connect({ provider_id, model })`. On success: sets `connectedFlash = true`, shows green confirmation, then calls `onConnected()` after 700 ms. On failure: shows error message. Button label toggles to "Connectingâ€¦" while in-flight. | `selected` and `selectedModel` must both be non-null; `connecting === false`. |
| L-07 | **Cancel** button | `LlmConnectModal` actions row | Click | Calls `onClose()` prop. No API call. | None. |
| L-08 | **Error banner** (conditional) | `LlmConnectModal` body | *(passive â€” no trigger)* | Displays `originalError` (yellow) or connection `error` (red). | `originalError` is set and `connectedFlash === false` (for original); `error` is set (for connection failure). |
| L-09 | **Connected flash** (conditional) | `LlmConnectModal` body | *(passive â€” no trigger)* | Shows "âœ“ Connected to {model} â€” formatting your ideaâ€¦" | `connectedFlash === true`. |
| L-10 | **Loading indicator** (conditional) | `LlmConnectModal` body | *(passive â€” no trigger)* | Shows "Detecting local AI providersâ€¦" | `loading === true`. |
| L-11 | **No-providers message** (conditional) | `LlmConnectModal` provider list | *(passive â€” no trigger)* | Displays guidance to install Ollama or set a cloud API key. | `providers.length === 0` and `loading === false`. |

---

## Surface: Header

| # | Control / Interaction | WHERE (Component) | User Trigger | Effect (API / State) | Preconditions |
|---|---|---|---|---|---|
| H-01 | **Start Wizard** button | `Header` status bar | Click | Calls `onStartWizard()` prop. No API call in this component. | None. |
| H-02 | **Missing assets badge** button | `Header` status bar | Click | Toggles `assetsOpen` state. When opened, renders a detail panel listing all missing asset names. No API call (data fetched on connect). | `connected === true` and `missingCount > 0`. |
| H-03 | **Missing assets detail panel** (conditional) | `Header` below status bar | *(passive â€” rendered when `assetsOpen === true`)* | Displays list of missing model asset names. | `assetsOpen === true` and `missingCount > 0`. |
| H-04 | **Disk usage** button | `Header` status bar | Click | Calls `onDiskUsageClick()` prop. No API call in this component. | `diskSizeFormatted` must be non-null (button only rendered then). |
| H-05 | **Clean Outputs** button | `Header` status bar | Click | Calls `onCleanupOutputs()` prop. Button label toggles to "Cleaningâ€¦" while `cleaningOutputs === true`. No API call in this component. | `cleaningOutputs === false` (button disabled otherwise). |
| H-06 | **Theme toggle** button | `Header` status bar | Click | Calls `onToggleTheme()` prop. Button label toggles between "â˜€ Light" and "â˜¾ Dark". No API call in this component. | None. |
| H-07 | **Logs** toggle button | `Header` status bar | Click | Toggles `logsOpen`. On first open (or re-open), calls `comfyuiApi.logs(40000)` to fetch last 40,000 chars of backend log. Renders log drawer with `<pre>` block. | None. |
| H-08 | **Log drawer â€” Refresh** button | `Header` log drawer header | Click | Calls `comfyuiApi.logs(40000)` again. Button label toggles to "Loadingâ€¦" while in-flight. | `logsOpen === true`. |
| H-09 | **Log drawer â€” Close** button | `Header` log drawer header | Click | Sets `logsOpen = false`. No API call. | `logsOpen === true`. |
| H-10 | **Log drawer content** (conditional) | `Header` log drawer body | *(passive â€” rendered when `logsOpen === true`)* | Displays log text, loading state, or error message. | `logsOpen === true`. |
| H-11 | **Scene count** indicator | `Header` status bar | *(passive â€” no trigger)* | Displays `{sceneCount} scenes`. | None. |
| H-12 | **ComfyUI queue info** indicator | `Header` status bar | *(passive â€” no trigger)* | Displays "{running} running Â· {remaining} queued" or "â€”". | `comfyuiStatus` must be non-null for queue numbers. |
| H-13 | **Queue time estimate** indicator (conditional) | `Header` status bar | *(passive â€” no trigger)* | Displays formatted time (e.g., "3m 42s est."). | `queueTimeEstimate != null` and `> 0`. |
| H-14 | **Connection pill** (conditional) | `Header` status bar | *(passive â€” no trigger)* | Shows "ComfyUI Connected" (green, pulsing dot) or "Disconnected" (red). | `comfyuiStatus` prop. |
| H-15 | **Asset status fetch** (auto) | `Header` `useEffect` | *(triggered on `connected` change)* | Calls `comfyuiApi.assets()` â†’ sets `assetsStatus`. No user trigger. | `connected === true`. |

---

## Cross-Surface State Transitions Summary

| Transition | Trigger | From â†’ To |
|---|---|---|
| Auto-cast in-flight | C-01 click | `autoCasting: false â†’ true â†’ false` |
| Voice preview in-flight | C-04 click | `previewing: null â†’ characterId â†’ null` |
| Preview audio rendered | C-04 success | `previewChar: null â†’ characterId`, `previewUrl: null â†’ url` |
| Frame search filter | F-02 input | `search: '' â†’ query` (client-side filter) |
| Scene selected for assignment | F-03 change | `selectedScene: '' â†’ sceneId` |
| LLM modal loading | L-01 mount | `loading: true â†’ false` |
| LLM provider selected | L-04 click | `selected: null â†’ provider`, `selectedModel: null â†’ modelId` |
| LLM model selected | L-05 click | `selectedModel: prev â†’ newModelId` |
| LLM connecting | L-06 click | `connecting: false â†’ true â†’ false` |
| LLM connected | L-06 success | `connectedFlash: false â†’ true` â†’ `onConnected()` after 700 ms |
| Assets panel open | H-02 click | `assetsOpen: false â†’ true` |
| Log drawer open | H-07 click | `logsOpen: false â†’ true` + `comfyuiApi.logs()` call |
| Log refresh | H-08 click | `logsLoading: false â†’ true â†’ false` |
| Theme toggled | H-06 click | `theme: 'dark' â†” 'light'` (via `onToggleTheme`) |
| Clean outputs in-flight | H-05 click | `cleaningOutputs: false â†’ true â†’ false` (via parent) |
